Initialize durable project memory for this repository.

Please:
1. Detect the project root and current git branch.
2. Create or update a global PROJECTS.md if a projects home is provided; otherwise ask where it should live.
3. Create a stable project context file using PROJECT_CONTEXT.md conventions unless an agent-specific file already exists.
4. Create a task ledger using .project/TASKS.md conventions unless an existing task ledger already exists.
5. Record current status, common commands, initial next step, blockers, and validation commands.
6. Do not overwrite existing project memory without summarizing what will change first.

中文等价：

请帮我初始化这个项目的长期项目记忆。创建/更新项目上下文、任务账本和全局 PROJECTS.md。不要覆盖已有记录，先说明会改什么。
