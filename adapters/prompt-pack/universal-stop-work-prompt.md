收工，记录当前进展。

请：
1. 检查 git status、git log -3、git diff --stat
2. 更新项目任务账本的 Snapshot、Current state、Stop-work note
3. 更新全局 PROJECTS.md 对应项目行
4. 如果只有收工记录改动，单独 commit，message 用 "Update <topic> wrap-up notes"
5. 最后输出当前状态、最新 commit、是否已 push、下次第一步


English equivalent:

```text
Wrap up and record current progress. Check git status/log/diff, update the task ledger and global PROJECTS.md, commit wrap-up notes if appropriate, and report current status, latest commit, push state, and next first action.
```
